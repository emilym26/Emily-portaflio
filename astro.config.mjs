import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://emilym26.github.io/Emily-portaflio', // ✔ corregido
  base: '/Emily-portaflio/', // ✔ corregido
});
