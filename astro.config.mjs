import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://emilym26.github.io/Emily-portaflio', // 👈 tu usuario real y nombre del repo
  base: '/Emily-portaflio/',
});
